import 'package:flutter/material.dart';

class CsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Welcome Customer Service")),
    );
  }
}
