import 'package:flutter/material.dart';

class TekPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Welcome Teknisi")),
    );
  }
}
